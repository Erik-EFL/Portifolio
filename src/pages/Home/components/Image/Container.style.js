import { Box } from '@mui/system';
import styled from 'styled-components';

const ImageContainer = styled(Box)`
  border-radius: 50%;
  box-shadow: 2px 2px 50px 0 rgba(0, 0, 0, 0.5);
  margin-top: 20px;
`;

export default ImageContainer;
